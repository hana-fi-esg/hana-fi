services:
  postgres:
    image: postgres:16
    container_name: hana-esg-postgres
    environment:
      POSTGRES_DB: esg_platform
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d esg_platform"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
